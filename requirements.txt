# Core framework
fastapi>=0.115
uvicorn[standard]>=0.30

# Data layer (SQLModel uses SQLAlchemy under the hood)
sqlmodel>=0.0.22
SQLAlchemy>=2.0
aiosqlite>=0.20

# Auth and security
passlib[bcrypt]>=1.7
python-jose[cryptography]>=3.3

# Pydantic (FastAPI pins a compatible version)
pydantic>=2.8

# Optional: testing
httpx>=0.27
pytest>=8.0
